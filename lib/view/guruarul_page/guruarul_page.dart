import 'package:flutter/material.dart';

class GuruarulPage extends StatelessWidget {
  const GuruarulPage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold();
  }
}